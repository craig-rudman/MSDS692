# macOS metadata
.DS_Store
.gitattributes

# Data pipeline outputs — large files, local only
# wfo_list.csv is tracked as the pipeline input
data/01_collection/
data/02_extraction/
data/03_cleaning/
# Geocode cache: local-only derived artifact. Safe to commit if you want
# reruns to skip the Nominatim calls, but ignored here for consistency.
data/cache/
# Geo caches: all regenerable. The two Census zips re-download on demand;
# wfo_coords.csv rebuilds via the NWS + Census geocoder APIs. Local-only.
data/geo/

# Personal study guide — local only, not part of the deliverable
report/defense_study_guide.html

# Python artifacts
*.pyc
__pycache__/
/docs
